# Playwright E2E (optional local overrides)
# Copy to .env.local or export in your shell — never commit real secrets for CI.
# CI uses GitHub Actions secrets with the same names.

E2E_BASE_URL=https://st-mary.rioassetmanagement.info/
E2E_EMAIL=your-admin@example.com
E2E_PASSWORD=your-password
